spring:
  port: 8080
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # oracle cloud? docker? ??? mysql container? ???? ??? ?? db ?? ?? ??? ????.
    url: jdbc:mysql://132.145.92.104:3306/menu?serverTimezone=UTC&characterEncoding=UTF-8
    username: musinsa
    password: 1234
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect

server:
  error:
    include-stacktrace: never

logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: trace
